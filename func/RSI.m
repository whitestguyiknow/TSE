function [R] = RSI(DS,dt,varargin)
% compute RSI
% example function call: RSI(DS,55,'max_ask','min_spread')
% returns: input dataset plus 2 additional columns with
%   RSI of mas_ask column in DS and RSI of min_spread column in DS

if(~iscellstr(varargin))
        error('optional arguments must be of type char');
end


end

